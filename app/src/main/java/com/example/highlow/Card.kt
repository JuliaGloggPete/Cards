package com.example.highlow

import android.widget.ImageView

class Card (var value: Int, var suits: String, var image: Int) {
// suits is never used in this app but left in as I find it easier to seperate the cards

}